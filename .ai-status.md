# AI Status

## Core Framework Versions
- **Next.js**: 15.5.12
- **React**: 19.2.4
- **TypeScript**: ^5

## Tooling Setup
- **ESLint**: Configured and active.
- **Prettier**: Configured with standard settings.
- **Strict Mode**: Enabled in `tsconfig.json`.

## Context Packing (Repomix)
- **Configuration**: `repomix.config.json` established with strict output settings (XML, line numbers, no empty lines).
- **Prioritization**: `.ai-status.md` is pinned as instructions. Documentation files (`docs/*`) are included in standard alphabetical order.

## Documentation
- **Task 0.2: Ground Truth Documentation** successfully seeded and verified.
    - `ARCHITECTURE.md`
    - `FUNCTIONAL_SPEC.md`
    - `DOMAIN_MODEL.md`
    - `UX_SPEC.md`
    - `API_CONTRACT.md`

## Design System Status
- **Design Tokens**: Configured in `app/globals.css` and `tailwind.config.ts`.
    - **Colors**: HSL variables (background, foreground, primary, secondary, accent, destructive, success, warning, etc.).
    - **Typography**: `var(--font-sans)` with fallback to Inter and sans-serif.
- **Components (Shadcn/UI)**:
    - `button`
    - `input`
    - `card`
    - `alert`
    - `toast`
    - `toaster`
    - `table`
    - `resizable`

## Application Structure
- **Route Groups**:
  - `(marketing)`: Public marketing pages (e.g., Home).
  - `(auth)`: Authentication pages (e.g., Login).
  - `(dashboard)`: Application dashboard pages (Overview, Strategies, Settings, Markets).

### App Directory Layout
```
app/
├── (auth)/
│   └── login/
│       └── page.tsx
├── (dashboard)/
│   ├── markets/
│   │   └── [marketId]/
│   │   │   └── board/
│   │   │       └── page.tsx
│   ├── overview/
│   │   └── page.tsx
│   ├── settings/
│   │   └── page.tsx
│   └── strategies/
│       └── page.tsx
├── (marketing)/
│   └── page.tsx
├── favicon.ico
├── globals.css
└── layout.tsx
```

## Infrastructure Status
- **Environment Variables**: Template created (`.env.example`). Mapped securely via AWS Amplify.
- **Client Providers**: Implemented in `app/providers.tsx` with `"use client"`.
    - **ThemeProvider**: `next-themes` (Attribute: class).
    - **QueryClientProvider**: Mock implementation (awaiting `@tanstack/react-query`).
    - **AuthProvider**: Mock implementation (awaiting Supabase Auth).
    - **Injection**: Wrapped in `app/layout.tsx` to isolate client-side logic.

## Current Phase Status
- Layout Shells (Marketing, Auth, Dashboard) are implemented and navigation links are wired and verified via E2E tests.
