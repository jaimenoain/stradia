version: 1
frontend:
  phases:
    preBuild:
      commands:
        - env | grep -e DATABASE_URL -e DIRECT_URL >> .env.production
        - PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci
        - npx prisma generate
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
